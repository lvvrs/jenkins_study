#!groovy
//Check Jenkins host
properties([disableConcurrentBuilds()])

pipeline {
    agent {
        label 'MASTER'
    }
    options {
        buildDiscarder(logRotator(numToKeepStr: '30', artifactNumKeepStr: '30'))
        timestamps()
    }
    stages {
        stage("Hostname") {
            steps {
                sh 'hostname'
            }
        }
        stage("Whoami") {
            steps {
                sh 'whoami'
            }
        }
        stage("Uptime") {
            steps {
                sh 'uptime'
            }
        }
    }
}